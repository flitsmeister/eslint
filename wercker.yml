box:
  id: flitsmeister/test-suite
  username: flitsmeister
  password: $DOCKER_PASS
deploy:
  steps:
    - script:
      code: |
        sed "s,\"version\": \"0.0.0\",\"version\": \"$WERCKER_GIT_TAG\"," -i $WERCKER_SOURCE_DIR/package.json
        npm publish
  after-steps:
    - slack-notifier:
        url: $SLACK_WEBHOOK
        username: Wercker